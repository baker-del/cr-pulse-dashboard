# Credentials — never commit these
google_credentials.json
.env
*.json

# Local database — cloud uses PostgreSQL via DATABASE_URL
database/kpi_dashboard.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS
.DS_Store

# Streamlit local secrets (cloud uses Streamlit Secrets UI)
.streamlit/secrets.toml
